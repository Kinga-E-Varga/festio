# List of details to fix in UI

- if an event is unpaid the Cancel the event shouldn't exist. Change color of 'Pay to publish btn'. Change color and text of This draft is unpaid.
- if an event is payed but hidden add a box (like the This draft is unpaid) warning to let the host know it's unreachable.
- dashboard - event card past: IF data deleted it shouldn't have buttons, link password, and only the free/standard/custom tag . If data is not deleted yet the edit btn should be disabled.
- check events page and event editing page responsiveness again
  event edit page > Invitation address > editable part should have the same light bg as other input fields
  - event edit page - protected whole box gets a border when clocked
